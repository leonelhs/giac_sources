# java interface for giac on linux 64 bits
# install the debian package from giac 1.1 from testing branch
# make the following command once
sudo cp libjavagiac.so /usr/lib
sudo ldconfig
# compile examples
# cd javagiac; javac *.java
# run one of the example
java javagiac.runme
# java javagiac.Visual (written by Diego Cueva)
# you can edit the example in the javagiac directory and compile it
cd javagiac && javac *.java && cd ..